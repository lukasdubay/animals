This is a Animal game project.

## How to use this project

Download node.js.

Instal ionic framework:
```bash
$ sudo npm install -g ionic cordova
```
Then, to run it, cd into `animals` and run:

```bash
$ ionic cordova platform add ios
$ ionic serve
```
